import { PagePipesPipe } from './page-pipes.pipe';

describe('PagePipesPipe', () => {
  it('create an instance', () => {
    const pipe = new PagePipesPipe();
    expect(pipe).toBeTruthy();
  });
});
